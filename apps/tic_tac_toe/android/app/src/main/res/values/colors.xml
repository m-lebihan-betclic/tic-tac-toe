<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- AppPalette.light().background. This is the window behind the app from the moment the
         process starts until Flutter paints its first frame; matching the in-app splash means
         the handover is invisible instead of a white flash. -->
    <color name="launch_background">#FAFAF8</color>
    <color name="ic_launcher_background">#0B0C0E</color>
</resources>