# tic_tac_toe

The binary. Everything of substance lives in `packages/` — this holds `main.dart`, the
composition of the `ProviderScope`, the `MaterialApp` that installs the design-system tokens as
`ThemeData` extensions, and the two platform folders.

See the repository root `README.md` to run it, and `CONVENTIONS.md` for the architecture.
