name: design_tokens
description: Raw design-system primitives — palette, type scale, spacing, radii, sizing, motion.
version: 1.0.0

environment:
  flutter: ">=3.27.0"
  sdk: ">=3.11.0 <4.0.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter

flutter:
  assets:
    - assets/
