name: app_providers
description: The composition root — every bindProviders() call in the app, in one list.
version: 1.0.0
publish_to: none

environment:
  flutter: ">=3.27.0"
  sdk: ">=3.11.0 <4.0.0"
resolution: workspace

dependencies:
  app_router:
  design_providers:
  design_tokens:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.2.1
  game_presentation:
  session_domain:
  setup_data:
  setup_presentation:
  splash_presentation:

dev_dependencies:
  build_runner: ^2.15.0
  flutter_test:
    sdk: flutter
  givn:
  riverpod_generator: ^4.0.3
  shouldly: ^0.5.0+1
